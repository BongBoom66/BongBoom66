-- ============================================================
-- បញ្ជាក់ អ្នកទទួល (បើខុសពីម្ចាស់គណនី) លើប័ណ្ណដឹកជញ្ជូន
-- Run ក្នុង Supabase SQL Editor
-- ============================================================
-- ភ្ញៀវខ្លះឲ្យអ្នកផ្សេងជាអ្នកទទួលទំនិញជំនួសខ្លួនឯង (ឧ. បុគ្គលិករោងចក្រ)
-- ជួរឈរនេះផ្ទុកឈ្មោះ/លេខទូរស័ព្ទអ្នកទទួល ដោយឡែកពីលេខទូរស័ព្ទម្ចាស់គណនី
-- (documents.customer_phone) ដែលនៅតែបង្ហាញដដែលលើប័ណ្ណដឹកជញ្ជូន។
alter table documents add column if not exists recipient_name text;
alter table documents add column if not exists recipient_phone text;
